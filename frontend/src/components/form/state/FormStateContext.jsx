/* ==========================================================
   Japhathakam Enterprise Form Engine
   Form State Context
-------------------------------------------------------------
   Provides the central React Context for the Form Engine.
========================================================== */

import { createContext } from "react";

const FormStateContext = createContext(null);

export default FormStateContext;
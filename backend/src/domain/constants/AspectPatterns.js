const ASPECT_PATTERNS = Object.freeze({

    DEFAULT: Object.freeze([1, 7]),

    KUJA: Object.freeze([1, 4, 7, 8]),

    GURU: Object.freeze([1, 5, 7, 9]),

    SHANI: Object.freeze([1, 3, 7, 10])

});

export default ASPECT_PATTERNS;